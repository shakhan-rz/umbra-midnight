# Screenshots (Level 1)

Drop the submission screenshots here:

- `compile.png` — the `compact compile` output showing the compiled circuit
  (`circuit "storeMessage" (k=6, rows=26)`).
- `deploy.png` — `yarn test:local` (or `test:preprod`) output showing
  `Contract deployed at: …` and the two passing tests.

These are referenced from the main [README](../README.md#-level-1-new-moon--submission).
